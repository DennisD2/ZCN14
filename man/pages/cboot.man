NAME
	cboot - card boot, boot ZCN from memory card

SYNOPSIS
	cboot

DESCRIPTION
	Card boot - boot ZCN from the memory card. If the card is not
	bootable, or there's no card in the slot, ZCN will say "Bad
	crd/drv/format" rather than booting.
